export interface Light {
    mode: string;
    brightness: number;
}

export interface Environment {
    temperature: number;
    humidity: number;
}
export interface AirConditioner {
    mode: string;
    temperature: number;
}